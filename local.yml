---
- hosts: localhost
  connection: local

  tasks:
    - name: Install CLI Tools
      become: true
      package:
        name:
          - bat
          - tmux
          - vim
          - zsh
    - name: Copy Configs
      copy:
        src: configs/
        dest: ~/
    - name: Clone oh-my-zsh
      git:
        repo: https://github.com/ohmyzsh/ohmyzsh.git
        dest: ~/.oh-my-zsh